name: Release

# On a vX.Y.Z tag, build static linux binaries (amd64 + arm64) with the version baked in,
# generate SHA256 checksums, and publish them as assets on the GitHub release for that tag.
on:
  push:
    tags: ['v*']

permissions:
  contents: write # required to create/update the release and upload assets

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Test gate (never publish binaries from a broken commit — same gate as CI)
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-clean:"; echo "$unformatted"; exit 1
          fi
          go vet ./...
          go run honnef.co/go/tools/cmd/staticcheck@v0.8.1 ./...
          go build ./...
          go build -tags gentoo ./...
          go test -race ./...
          go test -race -tags gentoo ./...
          go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...
          go run github.com/securego/gosec/v2/cmd/gosec@v2.28.0 -exclude=G304,G703,G706 ./...
      - name: Build static linux binaries (both editions, amd64 + arm64)
        run: |
          set -euo pipefail
          mkdir -p dist
          ver="${GITHUB_REF_NAME}"
          # vestibule is the Gentoo-zh edition: /pkg, /use, /bug, /news, /bbs and /arm
          # answer about Gentoo. gentoo-zhbot is the same bot for Linux communities in general,
          # where those commands carry a g prefix and the unqualified names stay free.
          for arch in amd64 arm64; do
            CGO_ENABLED=0 GOOS=linux GOARCH="$arch" \
              go build -trimpath -tags gentoo -ldflags "-s -w -X main.version=${ver}" \
              -o "dist/vestibule-linux-${arch}" ./cmd/bot
            CGO_ENABLED=0 GOOS=linux GOARCH="$arch" \
              go build -trimpath -ldflags "-s -w -X main.version=${ver}" \
              -o "dist/gentoo-zhbot-linux-${arch}" ./cmd/bot
          done
          ( cd dist && sha256sum vestibule-* gentoo-zhbot-* > SHA256SUMS )
          ls -l dist
      - name: Publish release with binaries + checksums
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          generate_release_notes: true
          files: |
            dist/vestibule-linux-amd64
            dist/vestibule-linux-arm64
            dist/gentoo-zhbot-linux-amd64
            dist/gentoo-zhbot-linux-arm64
            dist/SHA256SUMS
